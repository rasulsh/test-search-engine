# Runtime dependencies for the offline pipeline (developer GPU machine + CI mock).
numpy>=1.24
# PHP unserialize() for the byte-length-counted `feature` column.
phpserialize==1.3

# Real embedding (EMBEDDER=real) additionally needs, on the developer's GPU
# machine only (NOT installed in CI, which uses the deterministic mock embedder):
#   sentence-transformers>=2.2
